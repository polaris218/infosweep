import React from 'react';
import {
  Panel
} from 'components';

const ClientDetails = props => {
  return (
    <Panel
      header={(
        'Details'
      )}
    >
      <div></div>
    </Panel>
  )
}

export default ClientDetails;
